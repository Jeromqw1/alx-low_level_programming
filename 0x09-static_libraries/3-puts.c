#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _puts - prints a string to the standard output stream
 * puts - writes a string to the standard output stream, followed by a newline
 * @s: pointer to the string to be printed
 */
void _puts(char *s)
{
	puts(s);
}
