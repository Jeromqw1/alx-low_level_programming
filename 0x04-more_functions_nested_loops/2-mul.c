#include "main.h"
/**
 * mul - checks for a digit (0 through to 9)
 * @a: a - Variable
 * @b: b - Variable
 * Return: - Always 0 (success)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
